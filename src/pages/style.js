import styled from 'styled-components'

import { COLORS } from '../style'

export const StyleContainer = styled.div`
    width: 100vw;
    height: 100vh;
    background: ${COLORS[9]};
    color: ${COLORS[0]};
`